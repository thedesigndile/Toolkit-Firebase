# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value to automatically spin up more instances in response to
  # increased traffic. You can also set a minimum number of instances.
  # See https://firebase.google.com/docs/app-hosting/configure#scaling
  maxInstances: 2
